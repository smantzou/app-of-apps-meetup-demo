apiVersion: v1
kind: Service
metadata:
  name: {{ include "dummy.fullname" . }}
  labels:
    app: {{ include "dummy.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http-web
  selector:
    app: {{ include "dummy.name" . }}
